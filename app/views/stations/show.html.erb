<h2>current station</h2>
<%# <div class="flexbox vertical stretch">
        <% if @station.present? %>
          <%= render(partial: "stations/row") %>
        <% else %>
        <div class="empty">
          No stations found.
        </div>
        <% end %>
        
        <% if @bikes.present? %>
          <%= render(partial: "stations/bikerows", collection: @bikes, as: :bike) %>
        <% else %>
        <div class="empty">
          No bikes found.
        </div>


        <% end %> %>
